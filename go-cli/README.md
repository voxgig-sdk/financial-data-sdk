# financial-data-cli

AQL-driven CLI and REPL for the FinancialData SDK. Positional arguments are
joined into a single AQL expression and evaluated; with no arguments,
falls into an interactive REPL.

Built on `github.com/aql-lang/aql/eng/go` and the sibling Go SDK at `../go`.

## Build

```sh
go build -o financial-data-cli ./...
```

## Run

```sh
# One-shot: arguments form a single AQL expression
./financial-data-cli list basic_information
./financial-data-cli load 1 basic_information
./financial-data-cli load '{id:1}' basic_information

# REPL
./financial-data-cli
```

## Words

| Word     | Signatures                                   | Description                |
|----------|----------------------------------------------|----------------------------|
| `list`   | `[entity]` · `[query entity]`                | List records               |
| `load`   | `[entity]` · `[query entity]`                | Load a single record       |
| `update` | `[entity]` · `[query entity]`                | Update a record            |

`query` is either a Map (`{id:1}`) or a Scalar (`1`, treated as `{id:1}`).
`entity` is one of the SDK's entity names (auto-quoted as an atom).

## Entities

basic_information crypto_currency derivatives_data esg_data etf_data event_calendar financial_ratio financial_statement forex_data insider_trading institutional_trading investment_adviser market_data market_index market_new miscellaneous_data mutual_fund symbol_list

## REPL commands

- `:quit` / `:q` / `:exit` — exit the REPL
- `:help` / `:h` / `:?`     — show help

## Generated by

sdkgen `go-cli` target. See the target source under
`.sdk/src/cmp/go-cli/` in this repo, or upstream at
`github.com/voxgig/sdkgen/project/.sdk/src/cmp/go-cli/`.
